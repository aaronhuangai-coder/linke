# V0.72 Hardening-Status Web Panel TDD RED Report

- **角色**: implementer
- **状态**: RED (Failing as expected due to missing hardening-status implementation in Web Panel)
- **改动文件**:
  - [test/web-console.test.js](file:///Users/ah/linke/.worktrees/linke-v0.12-web-panel/test/web-console.test.js)
  - [docs/superpowers/reports/2026-07-06-v0.72-agy-task1.md](file:///Users/ah/linke/.worktrees/linke-v0.12-web-panel/docs/superpowers/reports/2026-07-06-v0.72-agy-task1.md)
- **运行命令**:
  ```bash
  node --test test/web-console.test.js
  ```
- **验证结果**:
  测试套件包含 10 个为 V0.72 hardening-status 网页控制面板编写的测试用例（包括 HTML 挂钩、wiring 字符串、CSS 状态选择器、pure-function 导出 and mock DOM 交互测试），所有 10 个测试均按预期失败：
  1. `HTML contains hardening-status panel with required data-testid hooks` - 失败：HTML 中缺少对应 `data-testid` 属性挂钩。
  2. `app.js contains wiring strings for hardening status` - 失败：`app.js` 中没有 `/api/hardening-status` 请求逻辑及 `buildHardeningStatusViewModel` 调用。
  3. `styles.css contains hardening status panel CSS status selectors partial and error` - 失败：`styles.css` 中缺少对应的 `hardening-status-panel[data-status="partial"]` 和 `hardening-status-panel[data-status="error"]` 样式选择器。
  4. `buildHardeningStatusViewModel is exported and handles unknown input` - 失败：`src/web/app.js` 中没有导出该纯函数。
  5. `buildHardeningStatusViewModel formats error status` - 失败：`src/web/app.js` 中没有导出该纯函数。
  6. `buildHardeningStatusViewModel formats partial status with whitelisted fields` - 失败：`src/web/app.js` 中没有导出该纯函数。
  7. `requests /api/hardening-status only once and renders partial status and whitelisted fields when refresh button is clicked` - 失败：点击刷新按钮未发出 `/api/hardening-status` API 请求。
  8. `renders error on non-2xx response from hardening-status` - 失败：API 请求出错时未按预期修改状态。
  9. `disables hardening-status refresh button during active request and restores afterwards` - 失败：请求中未禁用按钮。
  10. `does not start a second hardening-status request while one is in flight` - 失败：缺少 in-flight 防重复机制。
- **未验证内容**:
  - `src/web/app.js` 的具体逻辑实现
  - `src/web/index.html` 的具体 DOM 呈现
  - `src/web/styles.css` 的具体 CSS 展示
  - `/api/hardening-status` 的真实生产环境接口 data
- **风险**:
  - 该测试在 BypassSandbox 模式下执行以允许测试环境启动 localhost server 与 fetch 交互。在受限的严格 Sandbox 模式下运行会直接抛出 Local Host Loopback `EPERM` 网络限制异常，这与硬化功能本身无关。
